# `myerror`

A tiny recreation of [thiserror](https://github.com/dtolnay/thiserror) for learning purposes.

The macro itself is located in `macro-derive/`:
```
myerror
├── myerror-derive
│   └── src/lib.rs
└── src/main.rs
```
